[package]
name = "zed-diff-plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.1.0"

[profile.release]
opt-level = "z"
lto = true
strip = true
